// Estas funciones son ejemplos, aquí puedes desarrollar tus propias funciones.

export const filterData = (data, filterBy, value) => {
  const resultafoFiltro = []

  data.filter((element) =>{ 
    return element.color.value == ''


  } )
  
  return 'filterData';

};

export const anotherfilterData = () => {
  return [];
};


